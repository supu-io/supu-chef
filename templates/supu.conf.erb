description "<%= @name %>"

start on runlevel [2345]
stop on runlevel [016]

setuid vagrant
setgid vagrant

chdir /opt/supu-io/<%= @name %>

respawn # Will restart the app if it dies
exec ./<%= @name %>
